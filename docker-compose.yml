version: "3.9"

services:
  flutter:
    build: .
    container_name: flutter-dev
    volumes:
      - .:/app
      - flutter_pub_cache:/root/.pub-cache
    working_dir: /app
    stdin_open: true
    tty: true
    ports:
      - "8080:8080"

volumes:
  flutter_pub_cache:
